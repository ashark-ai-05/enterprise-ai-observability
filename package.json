{
  "name": "enterprise-ai-observability",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Enterprise AI observability, attribution, and effectiveness platform",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "undici": "^7.2.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.18.6",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.32.1",
  "license": "CC-BY-4.0",
  "bin": {
    "aiobs": "./dist/cli.js"
  }
}
